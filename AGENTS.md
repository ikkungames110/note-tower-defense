# もじもじ大作戦：開発ガイド

## ビジュアル方針（ユーザー指定・最優先）

- ユーザーは第2回コンセプト03（`public/concepts/v2/03.png`）の方向を選択。最新の調整版は `public/concepts/selected/03-clean.png`、比較画面は `public/concepts/selected/index.html`。
- 文字は普通に鉛筆で一度書いた自然で読みやすい筆跡にする。殴り書き、繰り返しなぞった線、ハッチング、荒いスケッチ表現は使わない。
- 文字の周囲にゴミのような破片、黒鉛の粉、散った点、消しかす、常時表示の攻撃線・軌跡を置かない。紙面は清潔に保ち、文字が立っていることを示す自然な影は残す。

- 教室の本物の机の上に一冊のノートが開いていて、その紙の上で、実際に鉛筆で書いたような文字そのものが移動・戦闘する。
- 「あいうえお」「ABCDE」に脚・腕・顔・目・色付けを加えない。文字はペラペラの黒鉛の筆跡だが、ノートのXY平面からZ軸方向に垂直に立つ。文字面は基本的にXZ平面で、X軸に沿って移動・戦闘する。紙面に寝かせない。厚い押し出し文字や擬人化キャラクターにしない。
- カメラは初回コンセプト06のように紙面すれすれの低い角度。画面の大部分をノートで埋め、後ろの教室は強くぼかす。「記憶の中の教室」のイメージ。
- ステージはとにかくリアルに。紙の繊維、ノートの折り目、机の木目、教室の自然光、周囲のブラーで「学生時代のノートの上」を表現する。
- 最初のカラフルな文字と顔・脚のあるノート風UIは方向違いとして却下された。現在のゲームは03調整版を元に、実写風背景・鉛筆の筆跡・紙色の最小限のUIへ更新済み。旧表現を見た目の正本として扱わない。
- ゲーム用背景は `public/assets/classroom-notebook.png`。調整版の文字と影を除去した画像で、生成プロンプトは `public/assets/README.md`。文字は `src/letters.js` の一画ずつのパスをCanvasとUIで共有する。
- 第2回の比較資料は `public/concepts/v2/index.html` の10案と評価画面。`public/concepts/v2/prompts.json` に内蔵image_genの生成プロンプトを保存。初回の平面に寝た文字の10案は `public/concepts/index-v1.html` に保存し、06は角度の参考としてのみ使用する。ユーザーが近い案を評価してから、採用する見た目をゲームに実装する。

## ゲームの方針

- ブラウザーで遊ぶ、ノート上の2D横方向タワーディフェンス。実写のような教室・机・ノートと、鉛筆の筆跡の文字を使う。
- 味方は「あ・い・う・え・お」、敵は「A・B・C・D・E」。文字の輪、直線、曲線、交差を攻撃モーションに生かし、文字として読める形を保つ。
- 左の自軍拠点を守り、右の敵拠点のHPをゼロにすると勝利。自軍拠点のHPがゼロになると敗北。移動・攻撃は自動、プレイヤーは召喚・資源強化・全体スキルを操作する。
- インクは時間と敵撃破で増える。召喚はインク消費・種類別クールダウン・味方30体の上限がある。インク強化は最大レベル5で回復速度と容量を増やす。
- 消しゴム砲は30秒チャージで敵全体へダメージと押し戻し。全3ステージのウェーブ制。初回クリアで次を解放し星を1個付与する。
- クリック・タップとキー1〜5、U、Q、Spaceに対応。停止・再開、倍速、図鑑、ガイド、結果、再挑戦を用意する。タブ非表示とダイアログ表示時は戦闘を止める。
- localStorageにクリア記録のみ保存し、途中の戦闘は保存しない。保存不可でもプレイできるようにする。

## 実装方針

- JavaScript ES Modules、HTML/CSS、Canvas 2D、Web Audio API、Vite。サーバー、アカウント、外部API、画像生成はゲーム実行時に必要ない。
- `src/game.js` はDOMから独立した戦闘シミュレーションと設定の正本。`src/render.js` はノート・文字・モーション・イベント演出。`src/main.js` はUI・入力・ループ・保存。`src/audio.js` は合成音。`src/style.css` はレスポンシブ表示。
- ロジックからイベントを発行し、描画と音が受け取る。ダメージは攻撃成立時点で適用し、短い飛翔演出は視覚表現とする。
- requestAnimationFrameで時間差を0.1秒以下に制限し、倍速適用後も1/60秒以下に分割してシミュレーションを更新する。
- Canvasは論理座標1200×440、縦横比を保持しDPRは最大2。実写風背景の上に透過Canvasを重ね、文字は一画ずつのパスから作った筆跡と紙面への投影影で構成する。移動・攻撃・反動・ヒットを小さな変形で表現し、顔・脚・色付けや破片は使わない。動きを減らす設定にも対応する。
- 音は初期ミュート。ユーザー操作でのみ有効にする。外部フォントや音源へのネットワーク接続に依存しない。
- UIは日本語。ボタンにラベル、キーボードフォーカス、ダイアログの閉じる操作を用意する。モバイルではカードを折り返し、スクロール可能にする。

## 検証と起動

- `npm install` → `npm run dev`。`npm run build` で本番ビルド。
- `npm test` で資源、召喚、戦闘、勝敗、停止、ステージ攻略を検証。
- `npm run test:browser` はPlaywrightで実際のUIとモバイル表示を検証（Chromiumのインストールが必要）。
- 戦闘の変更は意味のあるロジックテストを追加。描画変更はブラウザーで確認し、ビルド成功だけで見た目・動きの検証完了としない。
- 無断でコミット・公開せず、依頼範囲の実装と検証を進める。
